to get the correct module path work for mac package.
we modified the OpenNI framework to set the path.

OpenNI/XnModuleLoader.cpp

    XnStatus resolveModulesFile(XnChar* strFileName, XnUInt32 nBufSize)
    {
        XnStatus nRetVal = XN_STATUS_OK;
    
        nRetVal = xnGetOpenNIConfFilesPath(strFileName, nBufSize);
        XN_IS_STATUS_OK(nRetVal);
    
        //nRetVal = xnOSStrAppend(strFileName, "modules.xml", nBufSize);
    nRetVal = xnOSStrCopy(strFileName, "../Frameworks/modules.xml", nBufSize);
        XN_IS_STATUS_OK(nRetVal);
    
        return (XN_STATUS_OK);
    }